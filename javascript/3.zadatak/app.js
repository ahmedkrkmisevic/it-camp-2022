let recenica =
    "Nakon danasnjeg testa cemo znati ko ce nastaviti sa drugim ciklusom it campa"
let recenica4 = "";
for (let i = 0; i <= recenica.length - 1; i++) {
    if (recenica[i] === " a" && recenica[i] === "e" && recenica[i] === "o" && recenica[i] === "i" && recenica[i] === "u") {
        continue;
    } else {

    }
}


const numbers = [2, 25, 35, 45, 89, 100, -21, 53]
const noviniz = numbers.map(function (element) {
    if (element > 2, element * 7 && element > 2 < 8, element * 4 / 2) {
        return element < 10
    } else {
        return false
    }
});
console.log(noviniz);